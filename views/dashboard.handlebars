<h2 class="pb-4 pt-3 d-flex justify-content-center">{{username}}</h2>

{{#each posts as |post|}}
<div class="container-fluid mx-3">
{{> post-data post }}
</div>

{{else}}
<h6 class="pb-4">:C</h6>
{{/each}}
