
<h2 class="pb-4">{{username}}</h2>
<a href="/dashboard/create/"><button id="create-post" class="btn btn-primary">Upload</button></a>
<hr>

{{#if post.length}}

{{#each posts as |post|}}

{{> post-data post }}
</div>

{{else}}
<h6 class="pb-4">:C</h6>
{{/each}}

{{/if}}
